{
  "name": "5m-maker",
  "version": "0.1.0",
  "description": "Market-maker validation harness + bot for Polymarket crypto 5-minute markets",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "tsnode": "node --import \"data:text/javascript,import { register } from 'node:module'; import { pathToFileURL } from 'node:url'; register('ts-node/esm', pathToFileURL('./'));\"",
    "test": "vitest",
    "test:run": "vitest run",
    "record": "npm run tsnode -- scripts/recorder.ts",
    "simulate": "npm run tsnode -- scripts/simulate.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "js-yaml": "^4.1.0",
    "pino": "^8.17.0",
    "ws": "^8.15.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
